#This file is used to package the HarmonizedSystemCodes plugin.
#
#- It was generated by the InvenTree Plugin Creator tool - version 1.13.3
#- Ref: https://github.com/inventree/plugin_creator

[build-system]
requires = ["setuptools", "twine", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inventree-harmonized-system-codes"
description = "Support harmonized system codes against sales orders"
dynamic = ["version"]
authors = [
    { name = "Oliver Walters",email = "oliver.henry.walters@gmail.com" },
]
readme = "README.md"
license = "MIT"
keywords = ["inventree", "plugin"]
requires-python = ">=3.9"
dependencies = [
    # Enter your plugin library dependencies here
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Framework :: InvenTree",
]
[project.urls]
Homepage = "https://github.com/SchrodingersGat/inventree-harmoized-codes"

[project.entry-points."inventree_plugins"]
HarmonizedSystemCodes = "harmonized_system_codes.core:HarmonizedSystemCodes"

[tool.setuptools.packages.find]
where = [""]

[tool.setuptools.dynamic]
version = {attr = "harmonized_system_codes.PLUGIN_VERSION"} 

[tool.ruff]
exclude = [
    ".git",
    "__pycache__",
    "test.py",
    "tests",
    "venv",
    "env",
    ".venv",
    ".env",
]

src=["harmonized_system_codes"]
